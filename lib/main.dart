import 'package:flutter/material.dart';

import 'app/app.dart';

/// จุดเริ่มต้นของแอป StudySprout
///
/// เรียก [runApp] ด้วย [StudySproutApp] ซึ่งเป็น root widget
/// ส่วนธีม การ routing และโครงสร้างทั้งหมดถูกจัดการใน `app/` เรียบร้อยแล้ว
void main() {
  runApp(const StudySproutApp());
}
